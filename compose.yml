services:
  mcp-imagegen:
    image: ghcr.io/pvliesdonk/mcp-imagegen:latest
    build: .
    # Named volume permissions are fixed automatically by the entrypoint.
    # For bind-mount UID/GID mismatch, set PUID/PGID in your .env file:
    #   PUID=1001
    #   PGID=1001
    env_file: .env
    volumes:
      - service-data:/data/service
      - state-data:/data/state
    environment:
      FASTMCP_HOME: /data/state/fastmcp
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.mcp-imagegen.rule=Host(`${MCP_IMAGEGEN_HOST:-mcp-imagegen.local}`)"
      - "traefik.http.services.mcp-imagegen.loadbalancer.server.port=8000"

volumes:
  service-data:
  state-data:
